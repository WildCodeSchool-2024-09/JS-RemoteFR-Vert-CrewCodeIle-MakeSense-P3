{
  "name": "mn_template-p3",
  "version": "0.0.1",
  "description": "",
  "homepage": "https://github.com/wild code school/mn_template-p3#readme",
  "bugs": {
    "url": "https://github.com/wild code school/mn_template-p3/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wild code school/mn_template-p3.git"
  },
  "workspaces": ["client", "server"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "check": "biome check --error-on-warnings --no-errors-on-unmatched --staged . && npm run check-types --workspaces --if-present",
    "clean": "node ./bin/clean",
    "db:migrate": "npm run db:migrate --workspace=server",
    "db:seed": "npm run db:seed --workspace=server",
    "dev": "concurrently -c green,yellow -t \"HH:mm:ss\" -p \"{name} {time}\" \"npm:dev:*\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "prepare": "husky || true",
    "start": "npm run start --workspace=server",
    "test": "npm run test --workspaces --if-present"
  },
  "keywords": [],
  "author": "wild code school",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "concurrently": "^9.1.2",
    "husky": "^9.1.7",
    "validate-branch-name": "^1.3.2"
  }
}
